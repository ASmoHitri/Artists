kumuluzee:
  name: microservice-catalogs
  version: 1.0.0
  env:
    name: dev
  datasources:
      - jndi-name: jdbc/streamingDS
        connection-url: jdbc:postgresql://localhost:5432/streaming
        username: postgres
        max-pool-size: 20
  server:
    base-url : http://localhost:8081
    http:
      port: 8081